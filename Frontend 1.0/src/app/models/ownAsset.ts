import { Asset } from '../asset/asset';

export class ownAsset{
    assetId: string;
    assetcategory: string;
    beginDate: string;
    dueDate: string;
    requestedAsset: Asset;
    id: number;
}